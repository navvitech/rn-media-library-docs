### `getGenres()`

This function return an array of genres.

<p className='returns'>Returns:</p>

```ts copy showLineNumbers
interface Genre {
  _id: string;
  genre: string;
  numTracks: number;
  artwork: string;
}
```
