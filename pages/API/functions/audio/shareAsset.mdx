### `shareAsset(assetId: string)`

This is a function which is used to share an asset.
Calling this function opens a share sheet that has all the apps through which you can share the asset.

<p className='returns'>Returns:</p>

It returns nothing
