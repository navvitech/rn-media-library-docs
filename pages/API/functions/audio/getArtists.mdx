### `getArtists()`

This function return an array of artists.

<p className='returns'>Returns:</p>

```ts copy showLineNumbers
interface Artist {
  _id: string;
  name: string;
  numTracks: number;
  numAlbums: number;
}
```
